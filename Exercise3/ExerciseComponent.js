import React from 'react';
// Todo: Add your component code
// Important: Use export default function MyCmp() { ... }, don't define "export default" on a separate line!

function ExerciseComponent() {
    return <p> First exercise - done! </p>
}
// used the p tag to display the "First exercise - done!" sentence.
export default ExerciseComponent;
//always make sure to use the export default to export only the one value that you want to use so that other scripts 
//can import it for use.